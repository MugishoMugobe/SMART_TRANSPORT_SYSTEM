# For Render (or any host that runs its own WSGI server + Postgres).
# For Namecheap cPanel hosting, use requirements-cpanel.txt instead —
# see docs/DEPLOYMENT.md.
-r requirements-base.txt

gunicorn==26.0.0             # WSGI server
psycopg[binary]==3.3.4       # PostgreSQL driver
